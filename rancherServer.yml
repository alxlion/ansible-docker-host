---
- name: RancherServer
  hosts: rancherserver
  remote_user: root
  tasks:
  - name: RancherOS rancher/server
    docker:
      name: rancherserver
      image: rancher/server
      state: reloaded
      restart_policy: always
      ports:
      - "8080:8080"
